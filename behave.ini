[behave]
paths = features
steps_dir = features/steps

# To run only offline tests (no Spira connection needed):
#   behave --tags="~@integration"
#
# To run only integration tests (requires .env or pipeline env vars):
#   behave --tags="@integration"
#
# To run everything:
#   behave
